namespace LightningForge.Arcade.Game
{
    /// <summary>Which colour the local player is allowed to move.</summary>
    public enum ControlMode
    {
        /// <summary>Hot seat: one machine plays both sides.</summary>
        Both,
        WhiteOnly,
        BlackOnly
    }
}
